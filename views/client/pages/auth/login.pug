doctype html
html
    head
        meta(charset="UTF-8")
        title Đăng nhập
        link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous")
        link(href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet")
        link(href="/css/clientcss/login.css" rel="stylesheet")
            
    body
        .wrapper
            form#form-login(action=`${prefixUrl}/auth/login` method="post")
                h2
                    i.fas.fa-sign-in-alt
                    |  Đăng nhập

                .input-field
                    input(type="email" name="email" required)
                    label
                        i.fas.fa-envelope
                        |  Nhập email

                .input-field
                    input(type="password" name="password" required)
                    label
                        i.fas.fa-lock
                        |  Nhập mật khẩu

                .message #{message}

                .forget
                    a(href="/auth/forgot") Quên mật khẩu?

                button(type="submit")
                    i.fas.fa-sign-in-alt
                    |  Đăng nhập

                .register
                    p
                        | Chưa có tài khoản? 
                        a(href=`${prefixUrl}/auth/register`) Đăng ký